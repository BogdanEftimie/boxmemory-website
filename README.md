# BoxMemory — Landing Page

The marketing/landing page for the BoxMemory iOS app.

## Running Locally

```bash
cd website
python3 -m http.server 8080
```

Then open `http://localhost:8080` in your browser.

## Deployment

This site is deployed via GitHub Pages. See the repository Settings → Pages.
